export type data = {
    users: user[],
}

export type user = {
    firstName: string,
    lastName: string,
    DOB: Date,
    email: string,
    password: string,
    username: string,
    userID: number,
} 